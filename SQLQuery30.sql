--SQL Server SELECT DISTINCT - distinct custname
select distinct custname from product

--SQL Server SELECT DISTINCT - distinct city
select distinct city from product

--SQL Server SELECT DISTINCT - distinct state
select distinct state from product